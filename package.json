{
  "name": "disuware-module-collection",
  "version": "0.0.0",
  "description": "Disuware module collection",
  "private": true,
  "workspaces": [
    "./pkg/*"
  ],
  "scripts": {
    "lint": "eslint -c .eslintrc pkg",
    "test": "jest --config=jest.json",
    "test:watch": "jest --config=jest.json --watch"
  },
  "devDependencies": {
    "eslint": "~6.8.0",
    "eslint-config-google": "~0.14.0",
    "jest": "~25.1.0"
  }
}
